# Chỉ các dependency mà project thực sự dùng.
# Không pin cứng để pip tự chọn bản wheel phù hợp với Python đang chạy
# (tránh phải biên dịch từ source khi thiếu compiler).

fastapi
uvicorn
python-multipart      # xử lý upload file / form
jinja2                # render template trang demo
pillow                # xử lý ảnh
rembg[cpu]            # xoá nền (kéo theo onnxruntime, numpy, scipy, scikit-image, numba...)
